/** Nombre del header HTTP de autorización (Bearer token) */
export const AUTHORIZATION_HEADER = 'authorization' as const;

/** Timeout por defecto para llamadas HTTP entre servicios (ms) */
export const DEFAULT_HTTP_TIMEOUT_MS = 5000;
